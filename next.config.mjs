/** @type {import('next').NextConfig} */
const nextConfig = {
  // Habilitar strict mode para mejor desarrollo
  reactStrictMode: true,

  // Configuración de imágenes si se necesitan
  images: {
    domains: [],
  },
}

export default nextConfig
